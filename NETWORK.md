# الشبكة والاستهلاك — Network (v1.3.0)

**الشبكة والاستهلاك** صفحة مراقبة سلبية (passive) لاستهلاك الإنترنت على مستوى الجهاز كاملاً،
مع سقف يومي اختياري يقطع الإنترنت تلقائياً (kill-switch) وخطط بيانات بحساب يومي تلقائي.
كل شيء محلي — التطبيق نفسه **لا يستخدم أي إنترنت** لعملية المراقبة.

## ما الذي تراه في الصفحة؟

- **سرعة لحظية**: تنزيل (down) / رفع (up)، تُحدَّث دورياً أثناء وجود الصفحة.
- **استهلاك اليوم**: تنزيل اليوم / رفع اليوم / الإجمالي، منذ منتصف الليل المحلي.
- **حالة السقف**: المتبقي من السقف اليومي، وهل الحظر مفعّل الآن.
- **الخطة النشطة**: المخصص اليومي التلقائي + شريط التقدم (المستخدم مقابل الحصة).
- **سجل 14 يوماً**: جدول يوم بيوم (تنزيل/رفع/إجمالي)، الأحدث أولاً.

## كيف يعمل؟ (counters, sampler, rollover)

1. **العدادات (counters):** المصدر الأساسي هو عدّادات واجهة النظام التراكمية عبر `netstat -e`
   (بايتات مُستلمة/مُرسلة منذ بدء تشغيل النظام)، مع مسار احتياطي عبر `Get-NetAdapterStatistics`
   عند اختلاف لغة النظام. القراءة **سلبية تماماً**: لا اعتراض للحزم،
   لا تعريفات إضافية، لا صلاحيات مسؤول، ولا أي اتصال شبكي من التطبيق نفسه.
2. **المُعايِن (sampler):** يأخذ التطبيق لقطة دورية للعداد الخام ويحسب:
   - `السرعة = (العداد الآن − العداد السابق) ÷ زمن الفاصل` (الفروق السالبة تُصفَّر مع إعادة التأسيس بعد reboot).
   - `استهلاك اليوم += max(0, العداد الآن − العداد السابق)` لكل اتجاه على حدة.
   أخذ العينات يعمل فقط أثناء تشغيل التطبيق — الفترات التي كان فيها التطبيق مغلقاً لا تُقاس
   (العداد الخام يكون قد تقدّم، فيُعاد التأسيس عليه بدل احتساب فرق وهمي).
3. **إعادة التأسيس بعد إعادة التشغيل (rebase):** عدّادات Windows تُصفَّر عند كل reboot.
   إذا كان العداد الجديد **أصغر** من آخر لقطة محفوظة، يعتبر التطبيق ذلك إعادة تشغيل
   (وليس استهلاكاً سالباً): لا يُخصم شيء، ويُعاد التأسيس من القيمة الجديدة، ويستمر إجمالي اليوم من حيث توقف.
4. **التصفير عند منتصف الليل (rollover):** عند عبور منتصف الليل المحلي يُجمَّد إجمالي اليوم المنتهي
   في صف واحد بسجل الأيام (مرة واحدة فقط — محمي ضد التكرار بعد النوم/الاستيقاظ وتغيّرات التوقيت)،
   ثم يبدأ يوم جديد من الصفر مع حصة متجددة. يُحتفظ بآخر 90 يوماً ويعرض الجدول آخر 14 يوماً (الأحدث أولاً).

## السقف اليومي (daily cap)

- يُضبط بالغيغابايت (GB)، اختياري، ويُقارَن ضد **إجمالي اليوم (تنزيل + رفع)**.
- عند التجاوز — إذا كان الحظر التلقائي مفعّلاً والتطبيق يعمل **كمسؤول** — يُحظر إنترنت الجهاز كاملاً فوراً.
- الاستعادة التلقائية عند منتصف الليل المحلي مع حصة جديدة (إذا كان خيار الاستعادة مفعّلاً)، حتى لو بقي التطبيق مفتوحاً.
- خيار **استعادة عند الخروج** (restore-on-quit): إزالة الحظر عند إغلاق التطبيق بشكل طبيعي.

## الحظر وفكّه + تصميم الأمان (block/unblock flows + safety)

- **الآلية:** قواعد حظر صادرة/واردة في Windows Firewall باسم `DragonHub*` تُنشأ عند التجاوز
  وتُزال عند الاستعادة. تتطلب **التشغيل كمسؤول (UAC)** — بدون ترقية، تبقى المراقبة والسجل يعملان
  ويظهر تنبيه واضح بأن الحظر غير متاح، ولا تُطبَّق أي قاعدة ناقصة أبداً (all-or-nothing).
- **التدفقات:**
  - `تجاوز السقف → (تحقق admin) → إنشاء القواعد → حالة "محظور" + السبب والوقت`.
  - `منتصف الليل → إزالة القواعد → تجديد الحصة → حالة "مراقبة"`.
  - `خروج طبيعي + خيار الاستعادة → إزالة القواعد قبل الإغلاق`.
  - `بدء التشغيل → فحص قواعد `DragonHub*` اليتيمة → إزالتها (تنظيف الأعطال)`.
- **Fail-open دائماً:** أي تعطل أو قتل للعملية لا يترك الجهاز offline — تنظيف بدء التشغيل
  يزيل القواعد اليتيمة ويعيد الإنترنت دون تدخل. لا يوجد أي حظر دائم أو خفي.
- **فحص يدوي:** `netsh advfirewall firewall show rule name=all | findstr DragonHub`
  يجب ألا يعرض شيئاً بعد الاستعادة أو بعد إقلاع نظيف.

## خطط البيانات (data plans)

الحقول: **الاسم + الحصة (GB) + الدورة (يومية / أسبوعية / شهرية / مخصصة N أيام) + تاريخ البداية**.
منها يُشتق **المخصص اليومي التلقائي**:

```
allowance/day = quota ÷ days_in_cycle
```

- الدورة اليومية: القسمة على 1. الأسبوعية: على 7. المخصصة: على N.
- الشهرية: على طول الشهر الشمسي الحالي (28–31 يوماً) — وليست 30 ثابتة.
- التقدم: `المستخدم اليوم ÷ المخصص اليومي` مع المتبقي، ويُعاد حسابه يومياً.

### أمثلة حسابية (plan math)

| الخطة | الحساب | المخصص اليومي |
|---|---|---|
| 70GB شهرية (شهر 30 يوماً) | 70 ÷ 30 | **~2.3GB/يوم** |
| 70GB شهرية (فبراير 28 يوماً) | 70 ÷ 28 | **2.5GB/يوم** |
| 14GB أسبوعية | 14 ÷ 7 | **2GB/يوم** |
| 10GB مخصصة / 4 أيام | 10 ÷ 4 | **2.5GB/يوم** |
| 5GB يومية | 5 ÷ 1 | **5GB/يوم** |

## مفاتيح البيانات والخصوصية (data keys / privacy)

- كل بيانات الشبكة محلية في `electron-store` (ملف `%APPDATA%\DragonHub\dragonhub-data.json`، مفاتيح `net.*`):
  اللقطة الأخيرة للعداد الخام، إجمالي اليوم، السقف وخيارات الحظر، الخطط، وسجل الأيام.
- **لا سحابة، لا مزامنة هاتف، لا telemetry، لا اتصالات خارجية** لهذه الميزة — القيم الفاسدة
  (تعديل يدوي للملف) تُضبط/تُصفَّر بأمان بدل التعطل.
- تظهر البيانات نفسها أيضاً في جدول «أين بيانات المستخدم؟» في `README.md`.

## الحدود الصريحة (honest limits)

1. **إجماليات على مستوى النظام فقط** — لا تفصيل لكل تطبيق بدون driver/ETW (مقصود: لا تعريفات، لا تعقيد).
2. **العدادات تُصفَّر عند reboot** — يُعالَج بإعادة التأسيس، لكن استهلاك الفترات التي كان التطبيق فيها مغلقاً لا يُنسَب ليوم معين.
3. **حظر الجدار الناري يحتاج مسؤولاً (UAC)** — بدونه مراقبة فقط.
4. **الدقة تقريبية** (± حجم فاصل العينات وتوقيت النظام) — أداة ترشيد استهلاك، ليست فاتورة رسمية من مزود الخدمة.
5. **لا مزامنة سحابية/هاتف** — كل جهاز يتتبع استهلاكه وحده.

## استكشاف سريع

| العرض | التفسير |
|---|---|
| السرعة 0 رغم التصفح | حركة خفيفة تحت عتبة الفاصل — جرّب تحميل ملف كبير وراقب الارتفاع |
| اليوم لا يتصفّر | تحقق من توقيت النظام/المنطقة؛ التصفير عند منتصف الليل **المحلي** |
| الحظر لا يعمل | أعد التشغيل كمسؤول (كليك يمين ← Run as administrator) |
| الإنترنت مقطوع بعد تعطل | أعد فتح التطبيق — تنظيف بدء التشغيل يزيل القاعدة اليتيمة فوراً |
